
do pcall(function() local coreGui=game:GetService("CoreGui");if (coreGui and coreGui.RobloxGui) then local networkPause=coreGui.RobloxGui:FindFirstChild("CoreScripts/NetworkPause");if networkPause then networkPause:Destroy();end for _,obj in ipairs(coreGui.RobloxGui:GetDescendants()) do if (obj.Name:lower():find("network") and obj.Name:lower():find("pause")) then obj:Destroy();end end end end);local player=game.Players.LocalPlayer;local replicatedStorage=game:GetService("ReplicatedStorage");local runService=game:GetService("RunService");local userInputService=game:GetService("UserInputService");local scriptEnabled=true;local railLoopActive=false;local bondLoopActive=false;local healLoopActive=false;local CONFIG={BondDelay=0.05,BondBatch=1000,BondMax=1500,RailSpeed=1500,CameraHeight=15,AntiFallHeight=0,HealThreshold=0.5,SafetyCheckInterval=0.5};local screenGui=Instance.new("ScreenGui");screenGui.Name="NurhubGUI";screenGui.Parent=player:WaitForChild("PlayerGui");screenGui.ResetOnSpawn=false;local mainFrame=Instance.new("Frame");mainFrame.Name="MainFrame";mainFrame.Parent=screenGui;mainFrame.Size=UDim2.new(0,280,0,200);mainFrame.Position=UDim2.new(0,10,0,10);mainFrame.BackgroundColor3=Color3.fromRGB(20,20,25);mainFrame.BackgroundTransparency=0.08;mainFrame.BorderSizePixel=0;mainFrame.Active=true;mainFrame.Draggable=true;local shadow=Instance.new("UICorner");shadow.Parent=mainFrame;shadow.CornerRadius=UDim.new(0,12);local titleBar=Instance.new("Frame");titleBar.Parent=mainFrame;titleBar.Size=UDim2.new(1,0,0,40);titleBar.BackgroundColor3=Color3.fromRGB(30,30,35);titleBar.BackgroundTransparency=0.2;titleBar.BorderSizePixel=0;local titleCorner=Instance.new("UICorner");titleCorner.Parent=titleBar;titleCorner.CornerRadius=UDim.new(0,12);local title=Instance.new("TextLabel");title.Parent=titleBar;title.Size=UDim2.new(1, -70,1,0);title.Position=UDim2.new(0,15,0,0);title.BackgroundTransparency=1;title.Text="NURHUB AUTOBONDS V3.0";title.TextColor3=Color3.fromRGB(255,255,255);title.TextSize=14;title.TextXAlignment=Enum.TextXAlignment.Left;title.Font=Enum.Font.GothamBold;local closeButton=Instance.new("TextButton");closeButton.Parent=titleBar;closeButton.Size=UDim2.new(0,30,1,0);closeButton.Position=UDim2.new(1, -40,0,0);closeButton.BackgroundColor3=Color3.fromRGB(200,60,60);closeButton.BackgroundTransparency=0.2;closeButton.Text="✕";closeButton.TextColor3=Color3.fromRGB(255,255,255);closeButton.TextSize=18;closeButton.Font=Enum.Font.GothamBold;closeButton.BorderSizePixel=0;local closeCorner=Instance.new("UICorner");closeCorner.Parent=closeButton;closeCorner.CornerRadius=UDim.new(0,8);closeButton.MouseButton1Click:Connect(function() screenGui:Destroy();end);local toggleButton=Instance.new("TextButton");toggleButton.Parent=mainFrame;toggleButton.Size=UDim2.new(0.9,0,0,45);toggleButton.Position=UDim2.new(0.05,0,0,55);toggleButton.BackgroundColor3=Color3.fromRGB(76,175,80);toggleButton.Text="AUTO BONDS: ON";toggleButton.TextColor3=Color3.fromRGB(255,255,255);toggleButton.TextSize=16;toggleButton.Font=Enum.Font.GothamBold;toggleButton.BorderSizePixel=0;local toggleCorner=Instance.new("UICorner");toggleCorner.Parent=toggleButton;toggleCorner.CornerRadius=UDim.new(0,8);local scriptRunningStatus=Instance.new("TextLabel");scriptRunningStatus.Parent=mainFrame;scriptRunningStatus.Size=UDim2.new(0.9,0,0,25);scriptRunningStatus.Position=UDim2.new(0.05,0,0,110);scriptRunningStatus.BackgroundTransparency=1;scriptRunningStatus.Text="📌 SCRIPT: RUNNING";scriptRunningStatus.TextColor3=Color3.fromRGB(76,175,80);scriptRunningStatus.TextSize=13;scriptRunningStatus.TextXAlignment=Enum.TextXAlignment.Left;scriptRunningStatus.Font=Enum.Font.Gotham;local collectingStatus=Instance.new("TextLabel");collectingStatus.Parent=mainFrame;collectingStatus.Size=UDim2.new(0.9,0,0,25);collectingStatus.Position=UDim2.new(0.05,0,0,135);collectingStatus.BackgroundTransparency=1;collectingStatus.Text="💰 COLLECTING: ACTIVE";collectingStatus.TextColor3=Color3.fromRGB(76,175,80);collectingStatus.TextSize=13;collectingStatus.TextXAlignment=Enum.TextXAlignment.Left;collectingStatus.Font=Enum.Font.Gotham;local pingLabel=Instance.new("TextLabel");pingLabel.Parent=mainFrame;pingLabel.Size=UDim2.new(0.45,0,0,25);pingLabel.Position=UDim2.new(0.05,0,0,165);pingLabel.BackgroundTransparency=1;pingLabel.Text="📶 PING: -- ms";pingLabel.TextColor3=Color3.fromRGB(150,150,150);pingLabel.TextSize=12;pingLabel.TextXAlignment=Enum.TextXAlignment.Left;pingLabel.Font=Enum.Font.Gotham;local fpsLabel=Instance.new("TextLabel");fpsLabel.Parent=mainFrame;fpsLabel.Size=UDim2.new(0.45,0,0,25);fpsLabel.Position=UDim2.new(0.5,0,0,165);fpsLabel.BackgroundTransparency=1;fpsLabel.Text="🎮 FPS: --";fpsLabel.TextColor3=Color3.fromRGB(150,150,150);fpsLabel.TextSize=12;fpsLabel.TextXAlignment=Enum.TextXAlignment.Right;fpsLabel.Font=Enum.Font.Gotham;local versionLabel=Instance.new("TextLabel");versionLabel.Parent=mainFrame;versionLabel.Size=UDim2.new(1,0,0,20);versionLabel.Position=UDim2.new(0,0,1, -25);versionLabel.BackgroundTransparency=1;versionLabel.Text="Nurhub autoBonds V3.0";versionLabel.TextColor3=Color3.fromRGB(80,80,80);versionLabel.TextSize=10;versionLabel.Font=Enum.Font.Gotham;local function updateStatusUI() if scriptEnabled then toggleButton.Text="AUTO BONDS: ON";toggleButton.BackgroundColor3=Color3.fromRGB(76,175,80);scriptRunningStatus.Text="📌 SCRIPT: RUNNING";scriptRunningStatus.TextColor3=Color3.fromRGB(76,175,80);if bondLoopActive then collectingStatus.Text="💰 COLLECTING: ACTIVE";collectingStatus.TextColor3=Color3.fromRGB(76,175,80);else collectingStatus.Text="💰 COLLECTING: IDLE";collectingStatus.TextColor3=Color3.fromRGB(255,193,7);end else toggleButton.Text="AUTO BONDS: OFF";toggleButton.BackgroundColor3=Color3.fromRGB(200,60,60);scriptRunningStatus.Text="📌 SCRIPT: STOPPED";scriptRunningStatus.TextColor3=Color3.fromRGB(200,60,60);collectingStatus.Text="💰 COLLECTING: OFF";collectingStatus.TextColor3=Color3.fromRGB(150,150,150);end end local lastTime=tick();local frameCount=0;local ping=0;local function getPing() local stats=game:GetService("Stats");local network=stats:FindFirstChild("Network");if network then local pingData=network:FindFirstChild("Ping");if pingData then return math.floor(pingData.Value);end end return 0;end task.spawn(function() while true do task.wait(0.5);local currentTime=tick();local deltaTime=currentTime-lastTime ;local fps=math.floor(frameCount/deltaTime );frameCount=0;lastTime=currentTime;ping=getPing();if (fps>=60) then fpsLabel.Text="🎮 FPS: "   .. fps ;fpsLabel.TextColor3=Color3.fromRGB(76,175,80);elseif (fps>=30) then fpsLabel.Text="🎮 FPS: "   .. fps ;fpsLabel.TextColor3=Color3.fromRGB(255,193,7);else fpsLabel.Text="🎮 FPS: "   .. fps ;fpsLabel.TextColor3=Color3.fromRGB(200,60,60);end if (ping<=100) then pingLabel.Text="📶 PING: "   .. ping   .. " ms" ;pingLabel.TextColor3=Color3.fromRGB(76,175,80);elseif (ping<=200) then pingLabel.Text="📶 PING: "   .. ping   .. " ms" ;pingLabel.TextColor3=Color3.fromRGB(255,193,7);else pingLabel.Text="📶 PING: "   .. ping   .. " ms" ;pingLabel.TextColor3=Color3.fromRGB(200,60,60);end end end);runService.RenderStepped:Connect(function() frameCount=frameCount + 1 ;end);local function findRemoteEvent() local remotePaths={{"Shared","Universe","Network","RemoteEvent","Actionable"},{"Shared","Universe","Network","Actionable"},{"Remote","Actionable"},{"Actionable"}};for _,path in ipairs(remotePaths) do local current=replicatedStorage;local found=true;for _,name in ipairs(path) do current=current:FindFirstChild(name);if  not current then found=false;break;end end if (found and current:IsA("RemoteEvent")) then return current;end end for _,v in ipairs(replicatedStorage:GetDescendants()) do if (v:IsA("RemoteEvent") and (v.Name:lower():find("action") or v.Name:lower():find("bond"))) then return v;end end warn("Remote event not found! Game may have been updated.");return nil;end local remoteEvent=findRemoteEvent();local bondCoroutine;local function startAutoBonds() if bondCoroutine then task.cancel(bondCoroutine);end bondCoroutine=task.spawn(function() if  not remoteEvent then print("Auto Bonds disabled: Remote event not found");bondLoopActive=false;updateStatusUI();return;end print("Auto Bonds started!");bondLoopActive=true;updateStatusUI();while scriptEnabled do local success,err=pcall(function() for i=CONFIG.BondBatch,CONFIG.BondMax do if  not scriptEnabled then break;end remoteEvent:FireServer(i);task.wait(CONFIG.BondDelay);end end);if  not success then warn("Bond purchase error:",err);task.wait(5);else task.wait(1);end end bondLoopActive=false;updateStatusUI();end);end local function getRails() local rails={};local railSegments=workspace:FindFirstChild("RailSegments");if railSegments then for _,obj in ipairs(railSegments:GetDescendants()) do if (obj:IsA("BasePart") and ((obj.Name=="Guide") or obj.Name:lower():find("rail"))) then table.insert(rails,obj);end end end if ( #rails==0) then for _,obj in ipairs(workspace:GetDescendants()) do if (obj:IsA("BasePart") and obj:IsDescendantOf(workspace) and (obj.Name:lower():find("rail") or obj.Name:lower():find("track"))) then table.insert(rails,obj);end end end return rails;end local railCoroutine;local function startRailGrinding() if railCoroutine then task.cancel(railCoroutine);end railCoroutine=task.spawn(function() repeat task.wait();until player.Character and player.Character:FindFirstChild("HumanoidRootPart")  local rails=getRails();if ( #rails==0) then warn("No rails found! Rail grinding disabled.");railLoopActive=false;updateStatusUI();return;end railLoopActive=true;local root=player.Character.HumanoidRootPart;local humanoid=player.Character:FindFirstChild("Humanoid");if  not humanoid then railLoopActive=false;return;end local function findNearestRail(position) local nearest,nearestDist=nil,math.huge;for _,rail in ipairs(rails) do local dist=(rail.Position-position).Magnitude;if (dist<nearestDist) then nearestDist=dist;nearest=rail;end end return nearest,nearestDist;end local function findNextRail(currentPos,currentRail,visited) local lastDirection=(currentRail and currentRail.CFrame.LookVector.Unit) or Vector3.new(0,0,1) ;local bestRail,bestDot=nil,0.5;for _,rail in ipairs(rails) do if  not visited[rail] then local toRail=(rail.Position-currentPos).Unit;local dot=lastDirection:Dot(toRail);local distance=(rail.Position-currentPos).Magnitude;if ((dot>bestDot) and (distance<100)) then bestDot=dot;bestRail=rail;end end end return bestRail;end local originalCameraType=workspace.CurrentCamera.CameraType;workspace.CurrentCamera.CameraType=Enum.CameraType.Scriptable;local currentRail,_=findNearestRail(root.Position);local currentPos=(currentRail and currentRail.Position) or root.Position ;local visitedRails={};local isRunning=true;local function teleportToRail(rail) if (rail and player.Character) then local hrp=player.Character:FindFirstChild("HumanoidRootPart");if hrp then hrp.CFrame=rail.CFrame + Vector3.new(0,5,0) ;currentPos=hrp.Position;return true;end end return false;end task.spawn(function() while isRunning and scriptEnabled  do task.wait(CONFIG.SafetyCheckInterval);if  not scriptEnabled then break;end if player.Character then local hrp=player.Character:FindFirstChild("HumanoidRootPart");if (hrp and (hrp.Position.Y<CONFIG.AntiFallHeight)) then local nearestRail,distance=findNearestRail(hrp.Position);if (nearestRail and (distance<200)) then teleportToRail(nearestRail);end end end end end);while isRunning and scriptEnabled  do if ( not player.Character or  not player.Character:FindFirstChild("HumanoidRootPart")) then repeat task.wait();until player.Character or  not scriptEnabled  if  not scriptEnabled then break;end root=player.Character.HumanoidRootPart;humanoid=player.Character:FindFirstChild("Humanoid");if  not humanoid then break;end rails=getRails();end if currentRail then visitedRails[currentRail]=true;end local lastDirection=(currentRail and currentRail.CFrame.LookVector.Unit) or Vector3.new(0,0,1) ;local targetPos=currentPos-(lastDirection * 20) ;local distance=(targetPos-currentPos).Magnitude;local duration=distance/CONFIG.RailSpeed ;if (duration>0) then local startTime=tick();while ((tick() -startTime)<duration) and isRunning and scriptEnabled  do if  not player.Character then break;end local alpha=(tick() -startTime)/duration ;local currentPosLerp=currentPos:Lerp(targetPos,alpha);local hrp=player.Character:FindFirstChild("HumanoidRootPart");if hrp then hrp.CFrame=CFrame.new(currentPosLerp);local cameraPos=currentPosLerp + Vector3.new(0,CONFIG.CameraHeight,0) ;local lookPos=currentPosLerp + (lastDirection * 10) ;workspace.CurrentCamera.CFrame=CFrame.new(cameraPos,lookPos);end runService.RenderStepped:Wait();end end currentPos=targetPos;local nextRail=findNextRail(currentPos,currentRail,visitedRails);if  not nextRail then visitedRails={};nextRail,_=findNearestRail(currentPos);if  not nextRail then task.wait(1);rails=getRails();nextRail,_=findNearestRail(currentPos);end end currentRail=nextRail;task.wait();end workspace.CurrentCamera.CameraType=originalCameraType;railLoopActive=false;end);end local healCoroutine;local function startAutoHeal() if healCoroutine then task.cancel(healCoroutine);end healCoroutine=task.spawn(function() local healItems={"Bandage","MedKit","HealthPack","FirstAid","Bandages","MedicalKit","HealingItem","HealthPotion"};local function findHealItem() local backpack=player.Backpack;local character=player.Character;if backpack then for _,item in ipairs(backpack:GetChildren()) do for _,healName in ipairs(healItems) do if item.Name:find(healName) then return item;end end end end if character then for _,item in ipairs(character:GetChildren()) do for _,healName in ipairs(healItems) do if item.Name:find(healName) then return item;end end end end return nil;end local function useItem(item) if remoteEvent then remoteEvent:FireServer("UseItem",item.Name);return true;end if (item.Parent==player.Backpack) then local tool=item;tool.Parent=player.Character;task.wait(0.1);if tool:IsA("Tool") then tool:Activate();end return true;end return false;end healLoopActive=true;while scriptEnabled do task.wait(2);if player.Character then local humanoid=player.Character:FindFirstChild("Humanoid");if (humanoid and (humanoid.Health>0)) then local healthPercent=humanoid.Health/humanoid.MaxHealth ;if (healthPercent<CONFIG.HealThreshold) then local healItem=findHealItem();if healItem then if useItem(healItem) then print("Used",healItem.Name,"to heal!");task.wait(1);end else task.wait(5);end end end end end healLoopActive=false;end);end local function stopAllScripts() if bondCoroutine then task.cancel(bondCoroutine);bondCoroutine=nil;end if railCoroutine then task.cancel(railCoroutine);railCoroutine=nil;end if healCoroutine then task.cancel(healCoroutine);healCoroutine=nil;end bondLoopActive=false;railLoopActive=false;healLoopActive=false;updateStatusUI();end local function startAllScripts() if remoteEvent then startAutoBonds();else print("Auto Bonds: Remote event not found");end startRailGrinding();startAutoHeal();updateStatusUI();end toggleButton.MouseButton1Click:Connect(function() scriptEnabled= not scriptEnabled;if scriptEnabled then print("Script enabled");startAllScripts();else print("Script disabled");stopAllScripts();end updateStatusUI();end);player.CharacterAdded:Connect(function(character) if scriptEnabled then print("Character respawned, reinitializing...");task.wait(2);if railCoroutine then task.cancel(railCoroutine);railCoroutine=nil;end startRailGrinding();end end);task.spawn(function() while true do task.wait(30);pcall(function() local coreGui=game:GetService("CoreGui");if (coreGui and coreGui.RobloxGui) then local networkPause=coreGui.RobloxGui:FindFirstChild("CoreScripts/NetworkPause");if networkPause then networkPause:Destroy();end end end);end end);startAllScripts();print("=== Nurhub autoBonds V3.0 Loaded ===");print("✓ GUI Created");print("✓ Network Pause: Removed");print("✓ Auto Bonds: "   .. ((remoteEvent and "Active") or "Failed") );print("✓ Rail Grind: Active");print("✓ Auto Heal: Active");print("====================================="); end
